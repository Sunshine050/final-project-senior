# 🚀 วิธีรัน Frontend (Next.js)

## ❌ ปัญหา: 404 Not Found `/dashboard/dispatcher`

Error นี้เกิดจาก **Frontend ยังไม่รันอยู่** หรือรันอยู่แต่ port ไม่ถูกต้อง

---

## ✅ วิธีแก้: รัน Frontend

### ขั้นตอนที่ 1: เปิด Terminal ใหม่

เปิด Terminal/Command Prompt ใหม่ (แยกจาก backend)

### ขั้นตอนที่ 2: ไปที่โฟลเดอร์ frontend

```bash
cd frontend
```

### ขั้นตอนที่ 3: รัน Development Server

```bash
npm run dev
```

### ขั้นตอนที่ 4: เปิด Browser

เมื่อเห็นข้อความ:
```
  ▲ Next.js 14.0.4
  - Local:        http://localhost:3001
  - ready started server on 0.0.0.0:3001
```

เปิด Browser ไปที่: **http://localhost:3001** (หรือ port ที่แสดง)

---

## 📝 หมายเหตุ

- **Backend** รันที่ port **3000** (http://localhost:3000)
- **Frontend** รันที่ port **3001** (หรือ port อื่นที่ Next.js เลือก)
- Frontend จะเรียก API ไปที่ backend อัตโนมัติ

---

## 🔧 ถ้า Port 3001 ถูกใช้งานแล้ว

Next.js จะเลือก port อื่นอัตโนมัติ (เช่น 3002, 3003) ดูที่ terminal ว่าใช้ port อะไร

---

## ✅ หลังจากรัน Frontend แล้ว

1. เปิด Browser ไปที่ **http://localhost:3001** (หรือ port ที่แสดง)
2. ไปที่หน้า **Login**
3. ใช้ข้อมูล:
   - Email: `dispatcher@example.com`
   - Password: `password123`
4. หลังจากล็อกอินสำเร็จ จะ redirect ไปที่ `/dashboard/dispatcher` อัตโนมัติ

---

## 🛑 หยุด Frontend

กด `Ctrl + C` ใน terminal ที่รัน frontend

---

**พร้อมใช้งานแล้ว! 🎉**

