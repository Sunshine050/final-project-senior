# 📮 คู่มือการใช้ Postman Collection สำหรับทดสอบ API

## 📥 วิธี Import Collection

1. เปิด Postman
2. คลิก **Import** (มุมซ้ายบน)
3. เลือกไฟล์ `guardian-dispatch/Emergency_Care_API.postman_collection.json`
4. คลิก **Import**

---

## ⚙️ การตั้งค่าเบื้องต้น

### 1. ตั้งค่า Base URL

Collection จะใช้ตัวแปร `base_url` สำหรับ URL ของ backend:

1. คลิกที่ Collection **"Emergency Care API - Complete Collection"**
2. ไปที่แท็บ **Variables**
3. ตั้งค่า `base_url` เป็น:
   - **Local:** `http://localhost:3000`
   - **Production:** URL ของ backend จริง

---

## 🔐 วิธีล็อกอินและทดสอบ API

### ขั้นตอนที่ 1: สร้างบัญชีผู้ใช้ (ถ้ายังไม่มี)

#### สำหรับ Dispatcher (เจ้าหน้าที่ 1669):
1. เปิด folder **"🔐 Authentication"**
2. รัน request **"Register - Dispatcher"**
3. ตรวจสอบ response:
   ```json
   {
     "accessToken": "eyJhbGc...",
     "user": {
       "id": "...",
       "email": "dispatcher@example.com",
       "role": "dispatcher"
     }
   }
   ```
4. **Token จะถูกบันทึกอัตโนมัติ** ใน collection variables

#### สำหรับ Hospital Staff:
- รัน **"Register - Hospital Staff"**

#### สำหรับ Rescue Team:
- รัน **"Register - Rescue Team"**

#### สำหรับ Admin:
- รัน **"Register - Admin"**

---

### ขั้นตอนที่ 2: ล็อกอิน

1. เปิด folder **"🔐 Authentication"**
2. รัน request **"Login"**
3. แก้ไขข้อมูลใน Body:
   ```json
   {
     "email": "dispatcher@example.com",
     "password": "password123"
   }
   ```
4. คลิก **Send**
5. **Token จะถูกบันทึกอัตโนมัติ** และใช้ใน requests อื่นๆ ทันที

---

## ✅ ข้อมูลที่ถูกบันทึกอัตโนมัติ

หลังจากล็อกอินหรือสมัครสมาชิกสำเร็จ Collection จะบันทึกตัวแปรเหล่านี้:

- `access_token` - JWT token สำหรับ authentication
- `user_id` - ID ของผู้ใช้
- `user_role` - Role ของผู้ใช้ (dispatcher, hospital_staff, rescue_team, admin)
- `organization_id` - ID ขององค์กร (ถ้ามี)

**Token จะถูกใช้อัตโนมัติ** ใน requests ที่ต้องการ authentication

---

## 🧪 วิธีทดสอบ API ต่างๆ

### 1. SOS / Emergency APIs

1. เปิด folder **"🚨 SOS / Emergency"**
2. รัน requests ตามลำดับ:
   - **"Create Emergency"** - สร้างเคสฉุกเฉิน
   - **"Get All Emergencies"** - ดูรายการเคสทั้งหมด
   - **"Get Emergency by ID"** - ดูรายละเอียดเคส (ใช้ `emergency_id` ที่บันทึกไว้)
   - **"Assign Emergency"** - มอบหมายเคสไปโรงพยาบาล/ทีมกู้ชีพ
   - **"Update Status"** - อัปเดตสถานะเคส

### 2. Hospital APIs

1. เปิด folder **"🏥 Hospitals"**
2. รัน requests:
   - **"Get All Hospitals"** - ดูรายการโรงพยาบาล
   - **"Get Hospital by ID"** - ดูรายละเอียดโรงพยาบาล
   - **"Update Bed Availability"** - อัปเดตจำนวนเตียงว่าง

### 3. Organization APIs (Admin only)

1. ต้องล็อกอินด้วย role `admin` ก่อน
2. เปิด folder **"🏢 Organizations"**
3. รัน requests:
   - **"Get All Organizations"** - ดูรายการองค์กรทั้งหมด
   - **"Create Organization"** - สร้างองค์กรใหม่
   - **"Update Organization"** - แก้ไขข้อมูลองค์กร
   - **"Delete Organization"** - ลบองค์กร

### 4. Notifications APIs

1. เปิด folder **"🔔 Notifications"**
2. รัน requests:
   - **"Get Notifications"** - ดูรายการแจ้งเตือน
   - **"Mark as Read"** - ทำเครื่องหมายว่าอ่านแล้ว
   - **"Mark All as Read"** - ทำเครื่องหมายว่าอ่านทั้งหมดแล้ว

---

## 🔑 ข้อมูลล็อกอินตัวอย่าง

Collection มีข้อมูลตัวอย่างสำหรับแต่ละ role:

### Dispatcher
- Email: `dispatcher@example.com`
- Password: `password123`

### Hospital Staff
- Email: `hospital@example.com`
- Password: `password123`

### Rescue Team
- Email: `rescue@example.com`
- Password: `password123`

### Admin
- Email: `admin@example.com`
- Password: `password123`

---

## ⚠️ หมายเหตุสำคัญ

1. **ลำดับการทดสอบ:**
   - ต้องล็อกอินก่อนเสมอ (หรือสมัครสมาชิก)
   - Token จะถูกบันทึกอัตโนมัติ
   - Requests อื่นๆ จะใช้ token นี้โดยอัตโนมัติ

2. **การเปลี่ยน Role:**
   - ถ้าต้องการทดสอบ API ของ role อื่น ให้ล็อกอินด้วย role นั้น
   - Token เก่าจะถูกแทนที่ด้วย token ใหม่

3. **Organization ID:**
   - บาง API ต้องการ `organization_id`
   - ID จะถูกบันทึกอัตโนมัติหลังจากล็อกอิน
   - หรือสามารถดูได้จาก response ของ "Get All Organizations"

4. **Emergency ID:**
   - หลังจากสร้าง emergency แล้ว ID จะถูกบันทึกใน `emergency_id`
   - ใช้ ID นี้สำหรับ requests อื่นๆ ที่ต้องการ emergency ID

---

## 🚀 Quick Start

**วิธีเร็วที่สุดในการเริ่มทดสอบ:**

1. Import collection
2. ตั้งค่า `base_url` = `http://localhost:3000`
3. รัน **"Login"** (ใช้ email/password ตัวอย่าง)
4. ✅ Token ถูกบันทึกแล้ว! ตอนนี้สามารถทดสอบ API อื่นๆ ได้เลย

---

## 📝 Tips

- ดู **Console** ใน Postman เพื่อดู logs ของ token ที่บันทึก
- ตรวจสอบ **Collection Variables** เพื่อดูค่าที่บันทึกไว้
- ใช้ **Tests** tab เพื่อดู response และตัวแปรที่ถูกตั้งค่า

---

**พร้อมใช้งานแล้ว! 🎉**

