# 🔧 แก้ไขปัญหา "หน้าฟ้า" (Blue Screen Error)

## 🔍 วิธีตรวจสอบ Error

### 1. ดู Console Logs
- เปิด terminal ที่รัน Expo
- ดู error message ที่แสดง
- มักจะบอกว่า error เกิดที่ไฟล์ไหน บรรทัดไหน

### 2. ตรวจสอบใน Expo Go
- กด `j` ใน terminal เพื่อเปิด debugger
- หรือ shake device แล้วเลือก "Debug Remote JS"
- ดู error ใน browser console

### 3. ตรวจสอบ Network
- ตรวจสอบว่า ngrok ยังทำงานอยู่: `ngrok http 3000`
- ตรวจสอบว่า backend ยังทำงานอยู่: `cd backend && npm run start:dev`
- ทดสอบ API: เปิด browser ไปที่ `https://9d182bff63f9.ngrok-free.app`

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: Reload App
1. กด `r` ใน terminal (reload)
2. หรือ shake device แล้วเลือก "Reload"

### วิธีที่ 2: Clear Cache
```bash
cd emergency-app
npx expo start --clear
```

### วิธีที่ 3: ตรวจสอบ ngrok
```bash
# ตรวจสอบว่า ngrok ยังทำงานอยู่
# ถ้าไม่ทำงาน ให้รันใหม่:
ngrok http 3000
```

### วิธีที่ 4: ตรวจสอบ Backend
```bash
cd backend
npm run start:dev
# ตรวจสอบว่าแสดง "Application is running on: http://localhost:3000"
```

### วิธีที่ 5: ตรวจสอบ API URL
- เปิด `app.json`
- ตรวจสอบว่า `apiBaseUrl` ถูกต้อง
- ควรเป็น: `https://9d182bff63f9.ngrok-free.app`

---

## 🐛 ปัญหาที่พบบ่อย

### 1. Ngrok URL เปลี่ยน
**อาการ:** API ไม่ทำงาน
**แก้ไข:** 
- รัน `ngrok http 3000` ใหม่
- คัดลอก URL ใหม่
- อัปเดต `app.json`
- Restart Expo

### 2. Backend ไม่ทำงาน
**อาการ:** Network error
**แก้ไข:**
```bash
cd backend
npm run start:dev
```

### 3. Import Error
**อาการ:** Module not found
**แก้ไข:**
- ตรวจสอบ import paths
- ตรวจสอบว่าไฟล์มีอยู่จริง
- ลอง `npm install` ใหม่

### 4. Type Error
**อาการ:** Type mismatch
**แก้ไข:**
- ตรวจสอบ types ใน TypeScript
- ดู error message ใน console

---

## 📱 Quick Fix

```bash
# 1. ตรวจสอบ ngrok
ngrok http 3000

# 2. ตรวจสอบ backend
cd backend
npm run start:dev

# 3. Clear cache และ restart Expo
cd emergency-app
npx expo start --clear

# 4. Reload app (กด r ใน terminal)
```

---

## 🆘 ถ้ายังไม่ได้

1. **ดู error message ใน console**
2. **ส่ง error message มาให้ดู**
3. **ตรวจสอบว่า ngrok และ backend ทำงานอยู่**

