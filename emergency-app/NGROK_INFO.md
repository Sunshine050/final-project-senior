# 🔗 Ngrok Configuration

## ✅ URL ปัจจุบัน
```
https://9d182bff63f9.ngrok-free.app
```

## 📝 สิ่งที่ทำแล้ว
- ✅ อัปเดต `app.json` ให้ใช้ ngrok URL แล้ว
- ✅ App จะอ่าน API URL จาก `app.json` อัตโนมัติ

## 🚀 ขั้นตอนต่อไป

1. **Restart Expo:**
   ```bash
   cd emergency-app
   npx expo start --clear
   ```

2. **ตรวจสอบว่า API URL ถูกต้อง:**
   - ดู console log ว่าแสดง: `API Base URL: https://9d182bff63f9.ngrok-free.app`

3. **ทดสอบการเชื่อมต่อ:**
   - ลอง login
   - ลองส่ง SOS
   - ตรวจสอบว่าทำงานได้ปกติ

## ⚠️ หมายเหตุสำคัญ

### Ngrok URL จะเปลี่ยนเมื่อ:
- Restart ngrok
- ใช้ free tier (URL เปลี่ยนทุกครั้ง)

### ถ้า URL เปลี่ยน:
1. รัน `ngrok http 3000` ใหม่
2. คัดลอก URL ใหม่
3. แก้ไข `app.json`:
   ```json
   "apiBaseUrl": "https://URL_NEW.ngrok-free.app"
   ```
4. Restart Expo: `npx expo start --clear`

## 🎯 สำหรับพรีเซ็นต์

### สิ่งที่ต้องทำ:
1. ✅ รัน backend: `cd backend && npm run start:dev`
2. ✅ รัน ngrok: `ngrok http 3000`
3. ✅ อัปเดต app.json (ทำแล้ว)
4. ✅ Restart Expo

### ตอนนี้:
- ✅ ใช้ได้ทุกที่ (ไม่ต้อง WiFi เดียวกัน)
- ✅ ใช้ได้ทุก device (มือถือ, tablet)
- ✅ ใช้ HTTPS (ปลอดภัย)

## 🔄 ถ้าต้องการเปลี่ยน URL

ใช้ script:
```bash
node update-api-url.js https://NEW_URL.ngrok-free.app
```

หรือแก้ไข `app.json` โดยตรง:
```json
"extra": {
  "apiBaseUrl": "https://NEW_URL.ngrok-free.app"
}
```

