# 🎯 วิธีตั้งค่าสำหรับพรีเซ็นต์งาน

## ⚠️ ปัญหาปัจจุบัน
ตอนนี้ API URL เป็น `http://192.168.1.3:3000` ซึ่งเป็น IP local network
- ถ้าเปลี่ยน WiFi หรือไปที่อื่น → **จะเชื่อมต่อ backend ไม่ได้**

---

## ✅ วิธีแก้ไข: ใช้ ngrok (แนะนำสำหรับพรีเซ็นต์)

### ขั้นตอน:

1. **ติดตั้ง ngrok:**
   ```bash
   # Windows: ดาวน์โหลดจาก https://ngrok.com/download
   # หรือใช้ npm
   npm install -g ngrok
   ```

2. **รัน backend server:**
   ```bash
   cd backend
   npm run start:dev
   ```

3. **เปิด terminal ใหม่ และรัน ngrok:**
   ```bash
   ngrok http 3000
   ```

4. **คัดลอก HTTPS URL ที่ได้** (เช่น: `https://abc123.ngrok.io`)

5. **อัปเดต API URL ใน app:**
   - แก้ไข `app.json`:
     ```json
     "extra": {
       "apiBaseUrl": "https://abc123.ngrok.io"
     }
     ```
   - หรือใช้ environment variable:
     ```bash
     EXPO_PUBLIC_API_URL=https://abc123.ngrok.io
     ```

6. **Restart Expo:**
   ```bash
   npx expo start --clear
   ```

---

## 🔄 วิธีอื่นๆ

### วิธีที่ 2: ใช้ Public IP (ถ้ามี Static IP)
1. ตรวจสอบ public IP ของคุณ: https://whatismyipaddress.com
2. ตั้งค่า port forwarding บน router
3. ใช้ public IP แทน local IP

### วิธีที่ 3: ใช้ Domain Name
1. ซื้อ domain name
2. ตั้งค่า DNS
3. ใช้ domain แทน IP

### วิธีที่ 4: Deploy Backend ขึ้น Cloud
- Deploy backend ขึ้น Heroku, Railway, หรือ VPS
- ใช้ production URL ใน app

---

## 📱 สำหรับพรีเซ็นต์

### สิ่งที่ต้องเตรียม:
1. ✅ Laptop ที่รัน backend server
2. ✅ Laptop/Phone ที่รัน mobile app
3. ✅ เชื่อม WiFi เดียวกัน (หรือใช้ ngrok)
4. ✅ ngrok tunnel (ถ้าใช้ ngrok)

### ขั้นตอนก่อนพรีเซ็นต์:
1. รัน backend: `cd backend && npm run start:dev`
2. รัน ngrok: `ngrok http 3000`
3. อัปเดต API URL ใน app
4. Restart Expo
5. ทดสอบว่าเชื่อมต่อได้

---

## ⚡ Quick Fix สำหรับพรีเซ็นต์

```bash
# Terminal 1: รัน backend
cd backend
npm run start:dev

# Terminal 2: รัน ngrok
ngrok http 3000

# Terminal 3: อัปเดต app.json ด้วย ngrok URL แล้ว restart
cd emergency-app
npx expo start --clear
```

---

## 🎯 หมายเหตุ
- ngrok free tier มีข้อจำกัด (connection limit)
- ngrok URL จะเปลี่ยนทุกครั้งที่ restart (ถ้าใช้ free tier)
- สำหรับ production ควรใช้ domain name หรือ cloud hosting

