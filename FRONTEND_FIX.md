# 🔧 แก้ไข Frontend ให้ใช้ Ngrok URL

## ✅ สิ่งที่ทำแล้ว
- สร้างไฟล์ `.env.local` ในโฟลเดอร์ `frontend`
- ตั้งค่า `NEXT_PUBLIC_API_URL=https://8684c2291549.ngrok-free.app`

## ⚠️ ปัญหาที่พบ
- Frontend ยังได้ `POST /auth/login 404`
- Backend ได้รับ request ไปที่ `/rescue/dashboard` (ซึ่งเป็น frontend route)

## 🔍 สาเหตุ
Frontend ยังไม่ได้ restart หลังจากสร้าง `.env.local` ทำให้ยังใช้ค่าเก่า

## ✅ วิธีแก้ไข

### 1. หยุด Frontend
กด `Ctrl+C` ใน terminal ที่รัน frontend

### 2. Clear Next.js Cache
```bash
cd frontend
rm -rf .next
```

### 3. Restart Frontend
```bash
npm run dev
```

### 4. ตรวจสอบ Console
- เปิด browser console (F12)
- ควรเห็น: `🌐 API Base URL: https://8684c2291549.ngrok-free.app`
- ถ้ายังเห็น `http://localhost:3000` แสดงว่ายังไม่ได้โหลด `.env.local`

## 🔄 ถ้ายังไม่ได้

### ตรวจสอบว่า `.env.local` ถูกต้อง:
```bash
cd frontend
cat .env.local
```

ควรเห็น:
```
NEXT_PUBLIC_API_URL=https://8684c2291549.ngrok-free.app
NEXT_PUBLIC_WS_URL=https://8684c2291549.ngrok-free.app
```

### ตรวจสอบว่า Backend ทำงานอยู่:
```bash
cd backend
npm run start:dev
```

### ตรวจสอบว่า Ngrok ทำงานอยู่:
```bash
ngrok http 3000
```

## 📝 หมายเหตุ
- Next.js โหลด `.env.local` เมื่อ start เท่านั้น
- ต้อง restart frontend ทุกครั้งที่แก้ไข `.env.local`
- Environment variables ที่ขึ้นต้นด้วย `NEXT_PUBLIC_` จะถูก expose ไปที่ client

